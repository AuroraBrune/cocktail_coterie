<header>Create New Party</header>
<form>

    <div class="form-group">
        <label for="exampleFormControlSelect1">Related Service</label>
        <select type="submit" class="form-control" id="exampleFormControlSelect1">
            <option>Choose your Cocktail</option>
            {{#each cocktail}}
            <option>{{drinkName}}</option>
            {{/each}}
        </select>
    </div>

    <div class="form-group">
        <label for="Date">Date and Time</label>
        <!--look up how to get drop down date & time 
        also how to render & symbol for label-->
    </div>

    <div class="form-group">
        <label for="peronal-message">Personalized Message</label>
        <textarea class="form-control" id="personal-message" rows="3"></textarea>
    </div>

    <div class="form-group">
        <label for="cocktail-link">Your Cocktail Link</label>
        <!--find a way to get a sharebale link that shows the chosen cocktail
        and populate it in the input-->
        <input type="text" class="form-control" id="cocktail-link" placeholder="Example input placeholder">
    </div>

    <div class="form-group">
        <label for="zoom-link">Your Zoom Link</label>
        <!--   {{#if Zoom isdefined}} {{Zoom}}
    {{else}} -->
        <input type="text" class="form-control" id="zoom-link" placeholder="Another input placeholder">
    </div>

    <button type="submit" class="btn btn-primary">Share Invitation Link</button>
    {{#if invitation}}
    <div class="modal fade" id="share" tabindex="-1" role="dialog" aria-labelledby="myModalLabel" aria-hidden="true">
        <div class="modal-dialog modal-dialog-centered">
            <div class="modal-content">
                <div class="modal-header">
                    <button type="button" class="close" data-dismiss="modal" aria-label="Close"><span
                            aria-hidden="true">&times;</span></button>
                    <h4 class="modal-title" id="titleShare">Share</h4>
                </div>
                <div class="modal-body">
                    <input type="text" class="form-control" id="invitation-link" placeholder="party-link">
                    <button type="button" class="btn">Share</button>
                </div>
            </div>
        </div>
    </div>
    {{/if}}

    <!--look up what youtube share does-->
</form>